* Make max number of visible markers configurable by user
* Counter for total number of events
* Destination markers (yellow?) with arrows
* Limit maximal number of (source but NOT destination) markers
* Counter for number of hits for (destination and source) markers in hover tooltip
* Enlarge markers if the same event (IP) occurs multiple times,
  dont add the same marker again

* Bug: counting fails if elementFromPoint() hits an existing marker instead the underlying region

* Browser compatibility
